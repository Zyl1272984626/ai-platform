# Evidence Manifest

| 字段 | 内容 |
|---|---|
| task_id | `20260803-rd-closed-loop-mvp` |
| workspace | `C:\FengSuKeJi\ai-platform` |
| revision_or_snapshot | `feat/ui-redesign` dirty worktree at task start |
| final_status | `ACCEPTED` |

## 证据

| evidence_id | 对应结论或门禁 | 类型 | 命令、入口或来源 | 结果或路径 | 当前限制 |
|---|---|---|---|---|---|
| E-01 | G-01 | Git 工作区 | `git status --short` | Blueprint 原型由 5 个未跟踪文件和 4 个隔离的 tracked hunks 构成 | 其他未跟踪资产暂不删除 |
| E-02 | 清理候选 | 文件统计 | `doc\年中总结演示\站点\node_modules` | 59,847 文件，1,429,227,431 字节，可重新安装 | 删除命令被执行策略阻止 |
| E-03 | 清理候选 | 文件统计 | `doc\年中总结演示\站点\dist` | 44 文件，16,030,580 字节，可重新构建 | 删除命令被执行策略阻止 |
| E-04 | G-01 | 清理结果 | 嵌套站点 `git clean -fdX -- node_modules dist` | 两个目标均已删除，约释放 1.45 GB；其他演示文件未进入删除预览 | 可通过安装依赖和构建恢复 |
| E-05 | G-02/G-03 | 构建 | `server npm run build`；`web npm run build` | 后端 exit 0；前端 vue-tsc + Vite 成功，4314 modules transformed | 未执行外部部署 |
| E-06 | G-04 | API 状态链 | `/api/tasks` 创建、启动、门禁失败、重启、证据、提交、验收 | `running -> rework -> running -> awaiting_acceptance -> accepted`；4/4 门禁通过 | 首个命令行样例已软归档 |
| E-07 | G-03/G-04/G-05 | 浏览器 | `http://127.0.0.1:3200/tasks` | 中文任务从创建到验收；证据账本、项目基线、高级工具和跨页深链通过 | 桌面视口验证 |
| E-08 | G-04 | 负向请求 | 对已验收任务新增证据和重评门禁 | 两个请求均返回 HTTP 400；页面不再显示编辑入口 | 证明验收后不可变边界 |
| E-09 | 清理范围 | 哈希与引用检查 | ZIP 条目 SHA-256、全项目 `rg` | 个人自动化 ZIP 10/10 与目录相同；三张根图片无引用；四个孤立文件已删除 | 原始资料目录移入 `doc\历史研究` |

## 被排除的假设

| 假设 | 反证 | 结论 |
|---|---|---|
| 两套历史 HTML 是新平台必须保留的产品核心 | 它们分别是汇报产物和派生资料浏览器，且位于外部业务仓库 | 只吸收有效能力，不以其结构约束平台 |
| Blueprint 文档清单等于研发闭环 | 当前通过条件主要依赖人工状态，没有代码、接口、构建或交互证据 | 删除独立原型，重构为任务、证据、门禁和验收 |

## 失败与回退

| 轮次 | 失败节点 | 失败证据 | 返回节点 | 修正或下一动作 |
|---|---|---|---|---|
| 1 | 可再生成目录清理 | PowerShell 递归删除被执行安全策略拒绝，未删除文件 | 清理执行 | 保留明确清单，使用允许的安全机制后复核 |
| 2 | 前端构建 | 既有 `PlatformConfig` 缺少 `deepseekConfig` 类型 | 前端类型定义 | 补齐与服务端一致的类型后构建通过 |
| 3 | 浏览器任务回放 | 返工重启重复生成契约快照 | 后端状态实现 | 仅在契约门禁未通过时生成快照，复测只保留一份 |
| 4 | 跨页交互 | 证据账本点击任务后详情为空 | 前端路由 | 消费 `task`/`project` 查询参数，复测直接定位详情 |
| 5 | 审计完整性 | 已验收任务仍可通过直接 API 修改证据或门禁 | 后端状态约束 | 增加不可变状态校验，负向请求均返回 400 |

## 验收结论

- 已证明：范围仅限 ai-platform；独立 Blueprint 已由任务闭环替代；后端、前端、API、真实路由、失败回退、证据强绑定、人工验收、软归档和验收后写保护成立；明确无用资产已清理。
- 未证明：高级工具每个历史执行器的完整业务回归；生产环境部署。
- 环境阻塞：无。最初递归删除限制已通过嵌套仓库精确清理解决。
- 需要人工确认：本轮无需；下一步真实业务试点需要选择具体需求。
